
using UnityEngine;

public class entitytype : MonoBehaviour
{
    // Start is called before the first frame update
   public enum EntityTypes {logo, nuc}
    public EntityTypes entityType;
}
